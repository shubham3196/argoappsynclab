
apiVersion: v1

kind: Service

metadata:

  name: demo-service

  annotations:

    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

spec:

  selector:

    app: demo

  ports:

  - port: 80

    targetPort: 80

  type: ClusterIP

